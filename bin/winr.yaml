begin: |-
  require "strscan"
  scanner = StringScanner.new("test string")
tasks:
  regexp_pattern: |
    scanner.scan(/\w/)
  regexp_literal: |
    scanner.scan(/test/)
  string: |
    scanner.scan("test")
